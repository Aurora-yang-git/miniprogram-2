ai<view class="page-container">
  <t-navbar title="今日计划" left-arrow="{{false}}" custom-style="background-color: transparent;" />

  <view class="ui-card header-card">
    <view class="row-between">
      <view class="user-welcome">
        <view class="text-caption">Welcome Back</view>
        <view class="text-h2">保持专注 🔥</view>
      </view>
      <view class="streak-badge">
        <t-icon name="time" size="32rpx" style="margin-right:8rpx"/> {{myStats.streakDays}} 天连胜
      </view>
    </view>
  </view>

  <view class="dashboard-area row-center">
    <view class="ring-container">
      <view class="ring-track"></view>
      <view class="ring-progress" style="--p: {{reviewProgress}}%; background: conic-gradient(var(--c-primary) var(--p), transparent 0);"></view>
      <view class="ring-inner row-center">
        <view style="text-align:center">
          <view class="text-h1" style="font-size: 80rpx;">{{reviewCount}}</view>
          <view class="text-caption">待复习</view>
        </view>
      </view>
    </view>
  </view>

  <view style="padding: 0 60rpx; margin-bottom: 60rpx;">
    <button class="btn-base btn-accent" bind:tap="onStartReview">
      开始背单词 / 复习
    </button>
  </view>

  <view class="stats-row">
    <view class="ui-card stat-item">
      <view class="text-caption">今日积分</view>
      <view class="text-h2" style="color: var(--c-success);">{{myStats.todayScore}}</view>
    </view>
    <view class="ui-card stat-item">
      <view class="text-caption">累计掌握</view>
      <view class="text-h2" style="color: var(--c-primary);">1,024</view>
    </view>
  </view>

  <view class="rank-card" wx:if="{{rankTop && rankTop.length}}">
    <view class="rank-title">全服学霸榜</view>
    <view class="rank-list">
      <view class="rank-item" wx:for="{{rankTop}}" wx:key="openid">
        <view class="rank-left">
          <text class="rank-index">{{index + 1}}</text>
          <view class="rank-avatar">
            <image wx:if="{{item.avatarUrl}}" class="avatar-img" src="{{item.avatarUrl}}" mode="aspectFill" />
            <view wx:else class="avatar-fallback">{{item.initial || 'U'}}</view>
          </view>
          <text class="rank-name">{{item.nickname}}</text>
        </view>
        <text class="rank-xp">{{item.xp}}</text>
      </view>
    </view>
    <view class="rank-me" wx:if="{{myRank}}">
      我的排名：{{myRank}}
    </view>
  </view>
</view>
